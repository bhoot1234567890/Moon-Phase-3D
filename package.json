{
  "name": "threejsfinal",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "host": "vite --host",
    "test": "node --test moonphase.test.js",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=moon-phase-3d --branch=main"
  },
  "devDependencies": {
    "vite": "^4.3.9"
  },
  "dependencies": {
    "three": "^0.153.0"
  }
}
